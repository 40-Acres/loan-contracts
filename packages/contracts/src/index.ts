// @40-acres/contracts — typed ABIs + addresses for 40Acres Finance.
//
// Both `addresses` and the ABIs are generated. Run `pnpm build:package`
// from the workspace root to (re)create them before building.

export { addresses } from './addresses.js';
export type { Addresses, EnvAware } from './addresses.js';
export * from './generated.js';
