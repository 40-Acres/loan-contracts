---
"@40-acres/contracts": minor
---

Velodrome (optimism): add `marketplaces.vexy` (`0x6b47…6738`) to the address registry, matching the Aerodrome (base) Vexy marketplace entry. Pairs with on-chain registration of `VexyFacet` (`buyVexyListing`), `FortyAcresMarketplaceFacet` (`buyFortyAcresListing`), and the missing `isListingPurchasable` selector on the existing `MarketplaceFacet`.
